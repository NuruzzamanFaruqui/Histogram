% Generate random data
data = randn(1, 1000);

% Create a cumulative histogram
figure;
histogram(data, 30, 'FaceColor', 'green', 'EdgeColor', 'black', 'DisplayStyle', 'stairs', 'Normalization', 'cdf');
xlabel('Data');
ylabel('Cumulative Frequency');
title('Cumulative Histogram');
grid on;
